{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild ./index.js --bundle --platform=node --target=node18 --format=cjs --outfile=./dist/bundle.js --minify"
  },
  "devDependencies": {
    "esbuild": "^0.27.0"
  }
}
